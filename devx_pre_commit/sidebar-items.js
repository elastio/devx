initSidebarItems({"fn":[["install_self_as_hook","Copies the [`std::env::current_exe()`] file to `${project_root}/.git/hooks/pre-commit` That’s all you need to register a git pre-commit hook."],["locate_project_root","Searches for a project root dir, which is a directory that contains a `.git` dir as its direct child (it should also be the root of the project’s `Rust` crate or cargo workspace)."]],"struct":[["PreCommitContext","Represents the API entrypoint of the git pre-commit hook. It carries the list of the staged files and the project root path. Note that staged file paths are all relative to the project root path."]]});